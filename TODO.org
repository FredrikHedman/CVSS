* <2026-05-14 Wed> Shared cvss package expansion [3/3]
  1) [X] **Move `MetricValueDef`, `MetricDefinition`, `InvalidVectorError` to
     `cvss/vulnerability.py`** — Both `cvss2/vulnerability.py` and
     `cvss4/vulnerability_40.py` defined identical copies. Created
     `cvss/vulnerability.py` as the canonical location; both packages now import
     from there. Tests updated to import `MetricValueDef`/`InvalidVectorError`
     from `cvss.vulnerability`.

  2) [X] **Move `Metric` and `MetricValue` to `cvss/`** — `cvss2/metric.py` and
     `cvss4/metric.py` were byte-for-byte identical. Moved to `cvss/metric.py`
     and `cvss/metric_value.py`; all consumers updated to absolute imports.
     `pyproject.toml` updated to include `cvss` in packages/testpaths/include.

  3) [X] **Move `exit_with_error` to `cvss/cli.py`** — Both `cvss2/cvss_handlers.py`
     and `cvss4/cvss_handlers.py` defined the identical 2-line
     `_exit_with_error(e) -> NoReturn` helper. Moved to `cvss/cli.py` as
     `exit_with_error`; each handler imports it with `as _exit_with_error`.

* <2026-05-14 Wed> Output and type refactors [4/4]
  1) [X] **Export `EQLevels` as public type, fix `CVSS40Result` Protocol** (LOW) —
     Renamed `_EQLevels` → `EQLevels` in `cvss4/cvss_40.py`. Updated
     `cvss4/cvss_types.py` to import `EQLevels` via `TYPE_CHECKING` and declare
     `macro_vector` return type as `EQLevels` instead of the plain tuple. Callers
     holding a `CVSS40Result` reference now get named `.eq1`–`.eq6` access.

  2) [X] **`_show_flags` direct unit test** (LOW) — Added parametrized
     `test_show_flags` in `tests/cvss2/test_display.py` covering all six flag
     combinations (base, temporal, environmental, all, mixed, all-false). 168 tests
     pass (7 new).

  3) [X] **Document Exploit Maturity X→Attacked default** (LOW) — Added
     `"E (Exploit Maturity) absent or X defaults to Attacked (A)"` to the
     `--vulnerability` help string in `cvss4/cvss_parser.py`. Test in
     `tests/cvss4/test_cli.py` asserts the phrase "defaults to Attacked" appears
     in `--help` output.

  4) [X] **`render_output` → `format_output() -> str`** (LOW) — Renamed
     `render_output` to `format_output` in both `cvss2/cvss_output.py` and
     `cvss4/cvss_output.py`. Uses `io.StringIO + contextlib.redirect_stdout` to
     capture the existing print-based helpers; `main()` calls
     `sys.stdout.write(format_output(...))`. Tests updated: cvss4 tests now
     assert on return value directly (no `capsys`); cvss2 dispatch tests
     retain mocks but also check `isinstance(result, str)`.

* <2026-05-14 Wed> Suggested next refactors [3/3]
  1) [X] **Extract `capture_output` helper to `cvss/output.py`** (MEDIUM) —
     The identical `io.StringIO + contextlib.redirect_stdout` block in both
     `format_output` implementations was extracted to `cvss/output.py` as
     `capture_output(fn: Callable[[], None]) -> str`. Both packages import it and
     both `format_output` bodies are now 2-line dispatchers.

  2) [X] **`_render_base_only` extracted in cvss4** (LOW) — The inline non-verbose
     print block in `cvss4/cvss_output.py:format_output` was extracted to
     `_render_base_only(cvs)`, mirroring cvss2's split pattern. A parametrized
     `test_verbose_dispatch` test was added to `tests/cvss4/test_display.py`.

  3) [X] **`format_output` the primary tested surface** (LOW) —
     `test_display_score_prints_header` in `tests/cvss2/test_display.py` (which
     used `capsys` on the internal `display_score`) was replaced with
     `test_format_output_verbose_contains_base_table` that calls `format_output`
     end-to-end and asserts on the return value.

* <2026-05-14 Wed> Suggested future refactors [0/3]
  1) [ ] **Share `_print_group` table renderer in `cvss/output.py`** (MEDIUM) —
     cvss4's `_print_group(header, metrics)` and cvss2's
     `display_score(ScoreDisplayData)` both render aligned metric tables. A shared
     `print_metric_table(header, metrics, width)` primitive in `cvss/output.py`
     would let both packages use the same renderer.

  2) [ ] **Derive `ScoreDisplayData` headers from `MetricDefinition`** (LOW) —
     The header tuples in cvss2 verbose output are hand-coded strings (e.g.
     `"BASE METRIC"`); they could be derived from `BASE_DEFINITIONS` etc., making
     them drift-proof against future metric renames.

  3) [ ] **`format_output` end-to-end tests for known vectors** (LOW) —
     Focused `format_output` parametrized tests for known vectors with exact
     expected score strings would decouple format verification from the subprocess
     layer (currently covered only by `test_usecases.py` via the binary).

* <2026-05-13 Tue> Post-verbose/interactive pattern suggestions [0/0]
  (All items moved/completed — see 2026-05-14 entries above.)

* <2026-05-13 Tue> Post-split refactors for cvss2/cvss4 [10/12]
  1) [X] **CVSS-B/BT/BE/BTE score label in `cvss4/cvss_output.py`** (MEDIUM) —
     `_score_label()` checks `m.index != "X"` across `threat_metrics()` and
     `environmental_metrics()`; `render_output` now prefixes score with the
     correct label. Parametrized tests cover all four branches.

  2) [X] **Verbose output mode for `cvss4`** (MEDIUM) —
     `render_output(cvs, clarg: CvssArgs)` dispatches to `_render_verbose` when
     `clarg["verbose"]`. Verbose output shows all four metric groups in aligned
     tables (ABBREV column, width=44), then MacroVector EQ levels, score label,
     severity, and full vector.

  3) [X] **Remove `_40` suffix from `cvss4/vulnerability_40.py` accessors** (HIGH) —
     Renamed `base_metrics_40/threat_metrics_40/environmental_metrics_40/
     supplemental_metrics_40` → `base_metrics/threat_metrics/environmental_metrics/
     supplemental_metrics`. No call-site updates needed at time of change.

  4) [-] **Add interactive mode to `cvss4`** (MEDIUM) — **cancelled by design**.
     `cvss4` is intentionally a non-interactive vector scorer. Vectors are
     self-contained strings; interactive input adds no value over passing the
     complete vector string directly. The `--verbose` mode and `macro_vector`
     property already provide the inspection depth that interactive mode would
     have offered.

  5) [X] **Expose `macro_vector()` on `CommonVulnerabilityScore40`** (MEDIUM) —
     Added as a `@property` returning `_EQLevels` (NamedTuple). Declared in
     `CVSS40Result` Protocol as `tuple[int, int, int, int, int, int]` to avoid
     exposing the private type. Displayed in verbose output as
     `MacroVector EQ: [eq1, eq2, eq3, eq4, eq5, eq6]`.

* <2026-05-12 Mon> Findings after CVSS v4.0 spec/guide review [12/12]
  1) [X] **CVSS-B/BT/BE/BTE score nomenclature** (MEDIUM) — done in 2026-05-13 item 1.

  2) [X] **`render_output` ignores `--verbose` flag in `cvss4`** (MEDIUM) —
     done in 2026-05-13 item 2.

  3) [X] **No `--supplemental` interactive flag for v4.0** (MEDIUM) —
     superseded (interactive mode cancelled for `cvss4`; see item 4 above).

  4) [X] **Exception naming asymmetry: `InvalidBaseVectorError` vs `InvalidVectorError`** (LOW) —
     `cvss2/vulnerability.py` and `cvss2/cvss_handlers.py` now use `InvalidVectorError`.

  5) [X] **`_40` suffix redundant on `cvss4/vulnerability_40.py` accessors** (LOW) —
     done in 2026-05-13 item 3.

  6) [X] **MacroVector EQ levels not surfaced in output** (LOW) —
     done in 2026-05-13 item 5.

  7) [X] **Exploit Maturity "Not Defined" effective default undocumented** (LOW) —
     Done in 2026-05-14 item 3.

* <2026-05-12 Mon> Findings after protocol/scoring/test-hygiene refactors [3/3]
  1) [X] **Use `isinstance(cvs, CVSS40Result)` dispatch in `render_output`** (MEDIUM) —
     `render_output` in `cvss_output.py` dispatches to `_render_v40` via
     `cvs.version == "4.0"` — a runtime string check that bypasses the type system.
     Now that `CVSS40Result` is `@runtime_checkable`, replace this with
     `isinstance(cvs, CVSS40Result)` and narrow `_render_v40`'s parameter from
     `CVSSResult` to `CVSS40Result`. basedpyright will then enforce that
     `_render_v40` only receives valid v4.0 scorers (Protocol Narrowing Pattern).

  2) [X] **Make `CVSSResult` `@runtime_checkable` for symmetry** (LOW) —
     `CVSS40Result` is `@runtime_checkable`; `CVSSResult` is not. Decorating the
     base protocol too allows callers to use `isinstance(x, CVSSResult)` as a type
     guard (e.g. in handler dispatch code). Symmetric decoration is idiomatic when
     a sub-protocol is already decorated, and costs nothing at runtime.

  3) [X] **Deduplicate 2026-05-11 LOW items in TODO.org** (LOW) —
     The 2026-05-11 section's items 2 (`_BASE_CLARG`) and 3 (`display_score()`)
     were promoted verbatim into the 2026-05-12 sections and are now done in both
     places. Removing or collapsing the 2026-05-11 duplicates keeps the TODO log
     as a clean record without misleadingly unresolved entries.

* <2026-05-12 Mon> Findings after completing scoring/testing cleanup [3/3]
  1) [X] **Extend `CVSSResult` Protocol to cover all four metric group accessors** (MEDIUM) —
     `CVSSResult` in `cvss_types.py` declares only `base_metrics() -> list[Metric]`.
     `CommonVulnerabilityScore40` implements three additional typed accessors
     (`threat_metrics`, `environmental_metrics`, `supplemental_metrics`) that are
     absent from the protocol. Callers who hold a `CVSSResult` reference have no
     typed way to reach these accessors — they cannot inspect threat or
     environmental metrics without downcasting. Extend the protocol (or introduce
     a `CVSS40Result` sub-protocol that `CommonVulnerabilityScore40` also satisfies)
     so the full v4.0 public API is discoverable and checked by basedpyright.

  2) [X] **`_BASE_CLARG` as a pytest fixture in `test_display.py`** (LOW) —
     `_BASE_CLARG` is a module-level dict spread via `{**_BASE_CLARG, ...}` at
     every test call site. Converting to a `@pytest.fixture(name="base_clarg")`
     returning a fresh `CvssArgs` dict each call is the idiomatic pytest fixture
     pattern: it removes the manual spread, makes the test signature document
     its dependencies, and guarantees test isolation if the dict is ever mutated.

  3) [X] **Direct unit test for `display_score()` in `test_display.py`** (LOW) —
     `display_score` in `cvss_output.py` formats a `ScoreDisplayData` into tabular
     stdout. It is exercised only indirectly through the full CLI use-case fixtures
     (`test_usecases.py`). Adding a focused test that constructs a minimal
     `ScoreDisplayData` and asserts on `capsys.readouterr().out` — e.g. that
     the header label appears — applies the Arrange-Act-Assert pattern and
     completes direct unit coverage for `cvss_output.py`.

* <2026-05-12 Mon> Findings after v4.0 structure refactors (validate-on-construction, EQ functions, dict-based reading) [3/3]
  1) [X] **`_eq_contrib` and `_eq3eq6_contrib` should be module-level** (MEDIUM) —
     After R2 moved `_eq1`–`_eq6`, `_metric_eff`, and `_dist_from_specs` to module
     level, the scoring contribution helpers `_eq_contrib` and `_eq3eq6_contrib`
     remain as instance methods. They use `self._find_dist` (which itself only
     needs a candidate list and a dist-function). Extracting all three to module
     level would reduce `CommonVulnerabilityScore40` to a pure state-holder plus
     public interface — no private logic at all.

  2) [X] **`_EQ*_MAX` canonical vectors could be derived from metric definitions** (MEDIUM) —
     The `_EQ1_MAX`, `_EQ2_MAX`, etc. dictionaries are hardcoded strings like
     `"AV:N/PR:N/UI:N/"`. The most-severe value for each metric is always the
     FIRST entry in `MetricDefinition.values`. The spec's "highest severity vector"
     for each EQ level is determined by taking the most-severe allowed value per
     metric for that level. Deriving these from `BASE40_DEFINITIONS` (and modified
     equivalents from `ENVIRONMENTAL40_DEFINITIONS`) would make `cvss_40.py`
     self-consistent with the metric definitions and eliminate the risk of
     divergence if metric value sets change.

  3) [X] **`CommonVulnerabilityScore40.__init__` eagerly builds all 32 `Metric` objects** (LOW) —
     The `__init__` iterates over all four metric-definition tuples (32 total) to
     build `self._metrics`. Most callers only ever access `base_score` — they never
     call `base_metrics()`, `threat_metrics()`, `environmental_metrics()`, or
     `supplemental_metrics()`. Converting `self._metrics` to a
     `@functools.cached_property` that builds lazily on first access would avoid
     the upfront cost when only scoring is needed.

* <2026-05-11 Sun> Findings after _EQLevels/_accumulate_groups/cached_property refactors [3/3]
  1) [X] **Add accessor tests for `CommonVulnerabilityScore40`** — (MEDIUM)
     `threat_metrics()`, `environmental_metrics()`, and `supplemental_metrics()`
     are public accessors with no tests. Only `len(cvs.base_metrics()) == 11`
     is covered in `test_cvss_40.py`. Add parametrized tests asserting counts
     (1 threat, 14 environmental, 6 supplemental) and that the returned `Metric`
     objects carry the correct abbreviations (e.g. first threat metric abbrev is
     `"E"`). Fills a coverage gap in the v4.0 public API.

  2) [X] **`_BASE_CLARG` as a pytest fixture in `test_display.py`** — (LOW)
     `_BASE_CLARG` is a module-level dict spread via `{**_BASE_CLARG, ...}` at
     every call site. Converting to a `@pytest.fixture` returning a fresh
     `CvssArgs` each call is more idiomatic, removes the need for callers to
     remember to spread, and is consistent with how other test files use
     fixtures. No behavioural change; pure test-hygiene improvement.

  3) [X] **Add a unit test for `display_score()` in `test_display.py`** — (LOW)
     `display_score` in `cvss_output.py` is exercised only indirectly through
     the full use-case CLI fixtures (`test_usecases.py`). Adding a focused test
     that calls `display_score(minimal_ScoreDisplayData)` and asserts on
     `capsys.readouterr().out` (e.g. that the header text appears) would
     complete unit coverage for `cvss_output.py` and decouple output-format
     verification from the CLI integration layer.

* <2026-05-11 Sun> Findings after v4.0 handler/scoring refactors [3/3]
  1) [X] **`_eq_levels()` should return a `NamedTuple`** — `_eq_levels()` returns
     `tuple[int, int, int, int, int, int]`. In `base_score` it is destructured
     as `eq1, eq2, eq3, eq4, eq5, eq6 = self._eq_levels()` and six positional
     integers are passed to `_eq3eq6_contrib`. A `NamedTuple`:
     `class _EQLevels(NamedTuple): eq1:int; eq2:int; eq3:int; eq4:int; eq5:int; eq6:int`
     makes positional swap bugs impossible, the lookup key becomes
     `_LOOKUP[tuple(levels)]`, and callsites gain named access. High readability
     benefit relative to low implementation cost.

  2) [X] **Extract `_accumulate_groups` to unify both interactive paths** —
     (carry-over from 2026-05-11 item 5). `_interactive_v40` and the v2.10
     branch of `process_cmd_line_interactive` both use the same
     `if clarg["all"]: extend × N / elif clarg["base"]: extend; if temporal:
     extend; if environmental: extend` pattern. Extract
     `_accumulate_groups(clarg, groups)` where `groups` is a list of
     `(flag_key, metric_fn)` pairs, called with v4.0 and v2.10 metric accessors
     respectively. Removes the last structural duplication between the two
     interactive paths.

  3) [X] **`VulnerabilityVector40.parsed` should be a `cached_property`** —
     The `parsed` property iterates over all four metric-definition tuples on
     every call to build a complete dict with absent optionals set to `"X"`.
     Since `_raw` is assigned once in `__init__` and never mutated, the result
     is identical on every call. Replace `@property` with
     `@functools.cached_property`; the first call pays the iteration cost, all
     subsequent calls return the cached dict. Eliminates redundant work when
     handlers chain `.complete().parsed` after `_interactive_v40` builds its
     own `parsed` dict.

* <2026-05-11 Sun> Findings after v4.0 scoring/handlers/output addition [5/5]
  1) [X] **Duplicate vector dispatch in `cvss_handlers.py`** — `process_cmd_line_base`
     and `process_cmd_line_vulnerability` share identical structure: check
     `startswith(_V40_PREFIX)`, try `VulnerabilityVector40` / fall back to
     `VulnerabilityVector`, handle the same two exception types. Extract
     `_cvs_from_vector(vector: str, require_complete: bool = True) -> CVSSResult`
     to remove the duplication; `process_cmd_line_vulnerability` passes
     `require_complete=False` since it accepts partial vectors.

  2) [X] **Redundant `.valid()` before `.complete()` in handler call sites** —
     `VulnerabilityVector40.complete()` already calls `self.valid()` internally.
     All v4.0 handler call sites use `.valid().complete().parsed`, running
     validation twice. Remove the leading `.valid()` from every v4.0 call site
     in `cvss_handlers.py`. The same double-call exists in the v2.10 path
     (`vvec.valid().complete()`) and should be fixed there too.

  3) [X] **Data-driven `_dist_eq*` methods in `cvss_40.py`** — Six distance
     methods (`_dist_eq1` through `_dist_eq5`, `_dist_eq3eq6`) total ~80 lines
     with near-identical structure: look up effective metric value, sum float
     differences against a candidate vector dict, return `None` on any negative.
     Replace with a `_metric_diff(abbrev, level_dict, mv, eff_val)` helper and
     encode per-EQ metric→dict mappings in a module-level table. Reduces ~60
     lines of boilerplate and makes the per-EQ metric set explicit.

  4) [X] **Missing accessor tests in `test_cvss_40.py`** — `CommonVulnerabilityScore40`
     exposes `threat_metrics()`, `environmental_metrics()`, and
     `supplemental_metrics()` but none are tested. Only
     `len(cvs.base_metrics()) == 11` appears in the suite. Add parametrized
     tests asserting counts (1 threat, 14 environmental, 6 supplemental) and
     that the returned `Metric` objects carry the correct abbreviations
     (e.g. `E` for `threat_metrics()[0].name`).

  5) [X] **Shared metric-group accumulation in `_interactive_v40`** — The
     `if clarg["all"]: extend + extend + extend + extend` /
     `elif clarg["base"]: extend; if clarg["temporal"]: extend; if clarg["environmental"]:
     extend` structure in `_interactive_v40` is structurally identical to the
     v2.10 path in `process_cmd_line_interactive`. Extract an
     `_accumulate_groups(clarg, groups)` helper (where `groups` is a list of
     `(condition_key, metric_fn)` pairs) and call it from both interactive paths.
     `_accumulate_groups` now exists in `cvss2/cvss_handlers.py`; cvss4 has no
     interactive mode (cancelled by design).

* <2026-05-10 Sun> Findings after _show_flags/header-assert/render_output-test cleanup [3/3]
  1) [X] **`_show_flags` is not directly tested** — Done in 2026-05-14 item 2.
  2) [X] **`_BASE_CLARG` fixture in `test_display.py` is a module-level dict** —
     Sharing a mutable dict between tests via `{**_BASE_CLARG, ...}` spread is
     correct but verbose.  Converting to a `pytest.fixture` returning a fresh
     `CvssArgs` each call would be more idiomatic and remove the need for
     callers to remember to spread.
  3) [X] **`display_score` in `cvss_output.py` has no unit test** — The function
     formats a `ScoreDisplayData` into tabular stdout output.  `test_display.py`
     constructs `ScoreDisplayData` objects but never calls `display_score`.
     Adding a test that calls `display_score` with a minimal `ScoreDisplayData`
     and asserts on `capsys.readouterr().out` (e.g. that the header text appears)
     would complete unit coverage for `cvss_output.py`.

* <2026-05-10 Sun> Findings after private-helpers/if-elif/input-tests cleanup [3/3]
  1) [X] **`_generate_output` and `_generate_verbose_output` share the same
     `clarg["X"] or clarg["all"]` flag expansion pattern** — Both functions build
     an `entries: list[tuple[bool, ...]]` with the same three flag guards
     (`base or all`, `temporal or all`, `environmental or all`).  Extracting a
     helper `_show_flags(clarg) -> tuple[bool, bool, bool]` that returns the three
     booleans once would eliminate the duplication and make future flag changes
     apply in one place.
  2) [X] **`test_input.py` does not assert on printed metric headers** —
     `select_metric_value` prints a `+++...+++ <name> <abbrev> +++...+++` header
     and the metric value lines before each prompt.  Adding one assertion on
     `capsys.readouterr().out` in `test_select_valid_input` (e.g. that the
     metric name appears) would protect against accidental format regressions
     without coupling to the exact line layout.
  3) [X] **`render_output` is the only public function in `cvss_output.py` but
     has no direct unit test** — It is exercised only through the full CLI
     (`test_usecases.py`).  Adding a focused test that calls `render_output`
     directly (mocking `_generate_output` / `_generate_verbose_output` or
     capturing stdout) would give the dispatch branch independent coverage and
     faster feedback on verbose/non-verbose routing.

* <2026-05-10 Sun> Findings after handler/input/render-output cleanup [3/3]
  1) [X] **`render_output` and `generate_output`/`generate_verbose_output` are in the
     same module but at different levels of abstraction** — `render_output` is the
     public entry point; the other two are implementation helpers.  Making them
     private (`_generate_output`, `_generate_verbose_output`) would signal that
     callers should use `render_output` only, and prevent external code from
     bypassing the verbose/non-verbose branching.
  2) [X] **`process_cmd_line_interactive` has an implicit ordering dependency** —
     The `if clarg["base"]: ... if clarg["temporal"]: ... if clarg["all"]: ...`
     branches are mutually exclusive in practice (enforced by `_validate_flags`)
     but read as if they could all fire.  Rewriting as `if/elif/elif` makes the
     exclusivity explicit and avoids evaluating all branches on every call.
  3) [X] **`cvss_input.py` has no tests** — `select_metric_value` and `read_metrics`
     are the only public functions with no unit test coverage; they are exercised
     only by the interactive use-case fixture (`test_uc02_out.txt`).  Adding focused
     unit tests with `unittest.mock.patch("builtins.input", ...)` would decouple
     input-logic correctness from CLI integration testing.

* <2026-05-10 Sun> Findings after vulnerability-rule/dispatch/split cleanup [3/3]
  1) [X] **`process_cmd_line_interactive` in `cvss_prompt.py` is a handler, not a
     prompt** — `cvss_prompt.py` currently imports `CommonVulnerabilityScore`,
     `cvs_factory`, and all vulnerability accessors to support
     `process_cmd_line_interactive`.  These are scoring-layer imports, not prompting.
     Moving `process_cmd_line_interactive` to `cvss_handlers.py` (alongside the
     other two mode handlers) would leave `cvss_prompt.py` as a pure interactive
     selection library (`select_metric_value`, `read_metrics` only) with no scoring
     imports, and eliminate the `cvss_handlers → cvss_prompt` cross-module
     dependency for a single function.
  2) [X] **`main()` still branches on `verbose` inline** — The
     `if clarg["verbose"]: generate_verbose_output(...) else: generate_output(...)`
     pattern is the only conditional left in `main()`.  Extracting
     `render_output(cvs, clarg)` in `cvss_output.py` (which makes the
     verbose/non-verbose choice internally) would reduce `main()` to three
     statements: parse → dispatch → render.
  3) [X] **`cvss_prompt.py` name no longer fits after item 1** — Once
     `process_cmd_line_interactive` moves out, `cvss_prompt.py` contains only
     `select_metric_value` and `read_metrics`.  Renaming to `cvss_input.py`
     better describes pure terminal-input functions and removes the implication
     that the module contains a handler.

* <2026-05-10 Sun> Findings after orchestration refactor [3/3]
  1) [X] **Move `clarg["all"] = True` into `parse_and_validate()`** — `main()`
     still contains a post-dispatch mutation (`if clarg["vulnerability"]: clarg["all"]
     = True`) that sets output behaviour.  This semantic rule ("--vulnerability
     always shows all scores") belongs in `cvss_parser.py` alongside the other
     argument-interpretation logic.  After the move, `main()` is a pure
     orchestrator: parse → dispatch → output, with no state mutation.
  2) [X] **Move `process_cmd_line` to `cvss_handlers.py`** — The dispatch function
     is the last non-trivial code in `cvss.py` besides `main()`.  Moving it to
     `cvss_handlers.py` (where all three handlers live) would make `cvss.py`
     a minimal entry point containing only `main()` and its imports.  `cast`
     would move with it.
  3) [X] **Split `cvss_interactive.py` into input and output modules** —
     `cvss_interactive.py` now hosts two distinct concerns:
     interactive input (`select_metric_value`, `read_metrics`,
     `process_cmd_line_interactive`) and score output (`ScoreDisplayData`,
     `display_score`, `generate_output`, `generate_verbose_output`).
     Splitting into `cvss_prompt.py` (input) and `cvss_output.py` (output)
     would give each module a single clear responsibility.

* <2026-05-10 Sun> Findings after _validate_flags/read_metrics/default-drift cleanup [3/3]
  1) [X] **"no actionable flag" guard in `main()` could fold into `_validate_flags`** —
     `if not (clarg["interactive"] or clarg["base"] or clarg["vulnerability"]):`
     `parser.print_usage(); sys.exit(1)` is still in `main()`.  Moving it into
     `_validate_flags` (using `parser.error()` instead for consistency) would
     make `main()` a true 7-line orchestrator: parse → validate → dispatch →
     output.  Note: this changes the exit code from 1 to 2 and output from
     stdout to stderr for the "no flags" case — consistent with the other
     `parser.error()` calls.  No existing test asserts on this exit code.
  2) [X] **`process_cmd_line_base` and `…_vulnerability` duplicate error reporting** —
     Both functions have `except (InvalidBaseVectorError, ValueError) as e:`
     `print(e); sys.exit(1)`.  Extract a `_exit_with_error(e: Exception) -> NoReturn`
     helper to make the pattern explicit and typed (`NoReturn` from `typing`).
     Two call sites become single-liners.
  3) [X] **`process_cmd_line_interactive` belongs in `cvss_interactive.py`** — After
     moving `read_metrics`, all interactive helpers live in `cvss_interactive.py`,
     but `process_cmd_line_interactive` is still in `cvss.py`.  Moving it would
     make `cvss.py` a thin CLI layer (parser + dispatch only).  No circular
     import: `cvss_210` does not import from `cvss_interactive`, so adding
     `CommonVulnerabilityScore` to `cvss_interactive.py`'s imports is safe.

* <2026-05-09 Sat> Findings after _ParsedArgs/e2e-tests/drift-test/cast cleanup [3/3]
  1) [X] **`main()` has three distinct responsibilities** — flag validation, dispatch,
     and output rendering (~30 lines).  Extracting `_validate_flags(parser, clarg)`
     (which calls `parser.error()` and returns nothing) would make `main()` a clean
     ~10-line orchestrator: parse → validate → dispatch → output.  The extracted
     function is also independently testable.
  2) [X] **`read_metrics` belongs in `cvss_interactive.py`** — `read_metrics` calls
     `select_metric_value` (which lives in `cvss_interactive`) and is conceptually
     an interactive I/O operation.  Currently it lives in `cvss.py` alongside CLI
     wiring, making `cvss.py` import interactive I/O.  Moving `read_metrics` to
     `cvss_interactive.py` (and updating `process_cmd_line_interactive` to call it
     from there) would make `cvss.py` a pure CLI wiring layer.
  3) [X] **Default-value drift between `_ParsedArgs` and `build_parser()` is untested**
     — The existing `test_parser_keys_match_cvss_args` catches key-name drift but
     not default-value drift.  `_ParsedArgs` declares `verbose = False` and
     `vector = None`; if `build_parser()` changes a default, `make_clarg` would
     silently produce wrong values.  Extend the test (or add a sibling) to call
     `make_clarg(build_parser().parse_args([]))` and assert specific field values.

* <2026-05-09 Sat> Findings after str-signature/read_metrics/make_clarg cleanup [3/3]
  1) [X] **`test_temporal_without_base_rejected` and `…_environmental` duplicate
     `main()`'s logic** (carry-over from previous TODO item 4) — These tests
     mirror the validation if-conditions from `main()` inline rather than calling
     `main()` end-to-end.  Replace with end-to-end tests:
     `patch("sys.argv", ["cvss", "-it"])` + `main()`, asserting exit code 2 and
     the error message in stderr.  Dead code removed: the duplicated
     `if clarg["temporal"] and not clarg["base"]: parser.error(...)` blocks.
  2) [X] **Three places encode the same 8-field argument schema** — `build_parser()`,
     `_ParsedArgs`, and `make_clarg` all independently enumerate the 8 CLI flags.
     If a new flag is added, all three need updating.  Add a unit test that calls
     `vars(build_parser().parse_args([]))` and compares its key set to
     `CvssArgs.__required_keys__` to catch future drift at CI time.
  3) [X] **`process_cmd_line` has a type-narrowing RuntimeError guard that
     duplicates `main()`'s validation** — `elif clarg["base"]:` still needs
     `if vector is None: raise RuntimeError(...)` to satisfy the type checker,
     because `main()`'s `parser.error()` narrowing is not visible across the
     function boundary.  The guard could be removed if `process_cmd_line` calls
     were restructured so `main()` extracts `vector` and passes it directly to
     `process_cmd_line_base`, keeping `process_cmd_line` only as the interactive
     dispatcher.

* <2026-05-09 Sat> Findings after mutation/typed-args/guard/verbose-entries cleanup [4/4]
  1) [X] **`process_cmd_line_base` and `…_vulnerability` accept `CvssArgs` but only
     use one field each** — After the mutation refactor, `process_cmd_line_vulnerability`
     reads only `clarg["vulnerability"]` and `process_cmd_line_base` reads only
     `clarg["vector"]`.  Narrowing their signatures to `(vector: str) -> CVSS` and
     `(vulnerability: str) -> CVSS` respectively removes the unnecessary TypedDict
     dependency and makes each function's contract explicit.  Callers extract the
     string from `clarg` before the call.  Tests become simpler (no `CvssArgs`
     literal needed).
  2) [X] **`read_and_set` both mutates and returns the same list** — The function
     appends to `selected` AND returns it; callers rebind `selected` to the return
     value, making the in-place mutation redundant.  Simplify to a pure function
     `read_metrics(L: list[MetricDefinition]) -> list[str]` that returns a new
     list without requiring a `selected` input parameter.  Callers use `+=` or
     `selected.extend(read_metrics(...))` to accumulate results.
  3) [X] **`make_clarg` helper eliminates 32 remaining `reportAny` warnings**
     — The 8-field dict construction `clarg: CvssArgs = {"verbose": args.verbose,
     ...}` is currently inline in `main()`.  Extracting it to a private
     `_make_clarg(args: _ParsedArgs) -> CvssArgs` function makes it reusable from
     tests.  Tests can then call `_make_clarg(typed_args)` instead of building
     the dict from `_args.verbose` (which is `Any`), eliminating all `reportAny`
     in `tests/test_cli.py`.
  4) [X] **`test_temporal_without_base_rejected` and `…_environmental_without_temporal`
     duplicate `main()`'s validation logic** — These tests mirror the if-conditions
     from `main()` inline rather than calling `main()` end-to-end via `sys.argv`
     mocking.  If the validation logic in `main()` changes, these tests will not
     catch the regression.  Replace with end-to-end tests using
     `patch("sys.argv", [...])` to call `main()` directly.

* <2026-05-08 Fri> Findings after cast/error-handling/ScoreEntry/debug-print cleanup [4/4]
  1) [X] **`process_cmd_line_vulnerability` side-effect mutation** —
     Line 98: `clarg["all"] = True` mutates the caller's `CvssArgs` dict so that
     `generate_output` shows all three scores.  This is a hidden side effect:
     the caller (main) sees its dict modified, and `generate_output` silently
     depends on it.  The clean fix is to pass an explicit "show_all" flag or
     to derive what to show from the invocation path rather than mutating shared
     state.  Medium severity: currently harmless because `clarg` is not reused,
     but fragile.
  2) [X] **`argparse.Namespace` attributes are `Any` in `main()`** —
     `args.verbose`, `args.base`, etc. are all typed `Any` by argparse, causing
     40 `reportAny` warnings in basedpyright.  Introduce a private
     `_ParsedArgs(argparse.Namespace)` class with typed fields (`verbose: bool`,
     `vector: str | None`, …) and assign with one `# type: ignore[assignment]`.
     This makes every attribute access in the dict-construction block properly
     typed and eliminates all warnings without scattering ignores.
  3) [X] **`guard-then-catch` pattern in `process_cmd_line_base` and `…_vulnerability`** —
     The `if vector is None: raise ValueError(...)` checks sit inside the
     `try` block, so the handler catches and swallows them via `sys.exit(1)`.
     These guards protect against a programming error (missing required arg),
     not a user-input error, and should be placed BEFORE the `try` so that
     the distinction is explicit.
  4) [X] **`generate_verbose_output` duplicates the `clarg["X"] or clarg["all"]`
     flag logic** — Three separate `if clarg["base"] or clarg["all"]:` tests
     in `generate_verbose_output` replicate the same pattern as `generate_output`'s
     `entries` list.  Apply the same `list[tuple[bool, ScoreDisplayData]]` pattern
     for consistency and to make future flag changes apply to both output modes
     in one place.

* <2026-05-08 Fri> Findings after assert/except/DRY/NamedTuple/show-list cleanup [4/4]
  1) [X] **`process_cmd_line_vulnerability` has no error handling** —
     Unlike `process_cmd_line_interactive` and `process_cmd_line_base`,
     `process_cmd_line_vulnerability` calls `VulnerabilityVector(vulnerability)`
     and `vvec.valid()` with no try/except.  A bad vector string propagates as
     an unhandled `InvalidBaseVectorError` or `ValueError` to `main()`, where
     there is no handler.  Wrap the body in
     `try/except (InvalidBaseVectorError, ValueError) as e: print(e); sys.exit(1)`.
  2) [X] **`print("You need to use --help ...")` in `process_cmd_line` is dead code** —
     `main()` already guards `not (clarg["interactive"] or clarg["base"] or
     clarg["vulnerability"])` and exits before calling `process_cmd_line`.
     The else-branch in `process_cmd_line` can never be reached.  Replace with
     `raise RuntimeError("unreachable")` or remove it.
  3) [X] **`generate_output` show-list could pair flags with `ScoreEntry` directly** —
     `show = [clarg["base"] or clarg["all"], ...]` with a parallel `zip` is
     correct but the flag and score are linked only by position.  A list of
     `(flag, ScoreEntry)` pairs built in one step would make the relationship
     explicit without the separate `show` variable.
  4) [X] **`select_metric_value` debug-style print `###| |###`** —
     Line 49: `print("Selected metric value ###|", idx, "|###")` uses
     double-hash delimiters that look like debug/trace markers.  If intentional
     UI output, rename to something clearer; if legacy debug noise, remove it.
     Currently covered by `test_uc02_out.txt`, so removing it requires updating
     the fixture.

* <2026-05-08 Fri> Findings after override/@TypedDict/annotation cleanup [4/4]
  1) [X] **`assert` in production code in `cvss_210.py`** —
     Line 24: `assert len(self.__metrics) == len(metrics_seq), error_message`.
     The same pattern was already replaced with `raise ValueError` in
     `metric.py`.  Replace with `if len(...) != len(...): raise ValueError(...)`.
  2) [X] **`except Exception` → specific exception types in `cvss.py`** —
     `process_cmd_line_interactive` (line 65) and `process_cmd_line_base`
     (line 91) both catch `except Exception`.  After the CvssArgs refactor the
     only expected exceptions are `InvalidBaseVectorError` and `ValueError`.
     Narrowing to `except (InvalidBaseVectorError, ValueError)` prevents silent
     swallowing of unexpected errors.
  3) [X] **`list_of_scores` typed as `list[tuple[str, float, str]]`** —
     `generate_output` builds an anonymous list of 3-tuples and iterates with
     `name, val, vec = score`.  Introducing a `ScoreEntry = NamedTuple(...)` or
     a `@dataclass` would make the type explicit and remove the reliance on
     positional unpacking; aligns with the `ScoreDisplayData` dataclass pattern
     already used in the same module.
  4) [X] **`CommonVulnerabilityScore` hardcoded metric key lists** —
     `base_metrics()`, `base_vector`, `temporal_metrics()`, `temporal_vector`,
     `environmental_metrics()`, `environmental_vector` all duplicate key lists
     like `["AV","AC","Au","C","I","A"]`.  These are already encoded in
     `BASE_DEFINITIONS` etc. in `vulnerability.py`; derive the lists from the
     definitions to make `cvss_210.py` DRY and immune to future metric reorders.

* <2026-05-08 Fri> Findings from partition/ValueError/dataclass/print cleanup [4/4]
  1) [X] **Remove dead `if __name__ == "__main__": doctest.testmod()` blocks** —
     `cvss_210.py` and `vulnerability.py` still have them.  pytest runs doctests
     via `--doctest-modules`; these blocks add noise.  A 2-line removal each,
     same as the block already removed from `metric.py`.
  2) [X] **`except Exception` → specific exception types in `cvss.py`** —
     `process_cmd_line_interactive` (line 64) and `process_cmd_line_base`
     (line 87) both catch `except Exception`.  After the print-removal refactor
     the only expected exceptions are `InvalidBaseVectorError` and `ValueError`.
     Narrowing to `except (InvalidBaseVectorError, ValueError)` prevents silent
     swallowing of unexpected errors.
  3) [X] **`CommonVulnerabilityScore` hardcoded metric key lists** —
     `base_metrics()`, `base_vector`, `temporal_metrics()`, `temporal_vector`,
     `environmental_metrics()`, and `environmental_vector` all hard-code lists
     like `["AV","AC","Au","C","I","A"]`.  These duplicate the abbreviation
     order already encoded in `BASE_DEFINITIONS` (etc.) in `vulnerability.py`.
     Derive the lists from the definitions to make `cvss_210.py` DRY.
  4) [X] **`clarg: dict[str, Any]` → `argparse.Namespace` or `TypedDict`** —
     Five functions in `cvss.py` and two in `cvss_interactive.py` accept
     `dict[str, Any]`.  `vars(args)` in `main()` discards argparse's type
     information.  A `CvssArgs` TypedDict (keys: `base`, `temporal`,
     `environmental`, `all`, `interactive`, `verbose`, `vector`,
     `vulnerability`) would make every key access type-safe without changing
     runtime behaviour.

* <2026-05-08 Fri> Findings from Protocol / dict-comprehension refactors [4/4]
  1) [X] **Double error printing** — `_parse_vulnerability_string`, `valid()`,
     and `complete()` in `vulnerability.py` all `print(...)` before re-raising.
     Callers in `cvss.py` (`process_cmd_line_interactive`,
     `process_cmd_line_base`) also `print(e)` on the caught exception, so every
     error message appears twice.  Remove the `print` calls from the raising
     site; let the top-level handler be the single place that reports errors.
  2) [X] **`part.split(":")[0]: part.split(":")[1]` double split** — The new
     dict comprehension in `_parse_vulnerability_string` calls `split(":")` twice
     per part.  Replace with `part.split(":", 1)` unpacked into `k, v`, which is
     both cheaper and more readable.  Using `maxsplit=1` also correctly handles
     values that contain colons.
  3) [X] **Dead `if __name__ == "__main__": doctest.testmod()` blocks** —
     `cvss_210.py` and `vulnerability.py` still have them.  pytest already runs
     doctests via `--doctest-modules`; these blocks add noise with no benefit.
     Remove them (same as the block removed from `metric.py` in Refactor 1).
  4) [X] **`clarg: dict[str, Any]` → use `argparse.Namespace` directly** —
     `main()` calls `vars(args)` and passes the resulting `dict[str, Any]` to
     every `process_cmd_line_*` function.  Using `args` (a `Namespace`) directly
     gives typed attribute access; alternatively a `TypedDict` makes the keys
     explicit.  Either removes the `Any` annotations on `clarg` parameters.

* <2026-05-08 Fri> Refactor findings from type annotation work [7/7]
  1) [X] **Replace docopt with click or argparse**
     docopt is unmaintained and emits SyntaxWarning on Python 3.12+
     (visible on every `cvss --version` call).  click was already noted
     in the 2014 TODO; argparse is stdlib.
  2) [X] **MetricValue → @dataclass(frozen=True) or NamedTuple**
     The class is a pure immutable value object with no logic beyond
     property access.  A frozen dataclass (or NamedTuple) would eliminate
     all manual __init__, __repr__, and property boilerplate.
  3) [X] **Unify *_SCORE strings and *_metrics() data (DRY)**
     BASE_SCORE et al. and base_metrics() both encode the same metric
     abbreviations (originally noted 2014-03-08 TODO).  A MetricDefinition
     dataclass with name, abbreviation, weight values, and allowed
     abbreviation list would be a single source of truth.
  4) [X] **display_score argument shapes → dataclass or TypedDict**
     Five positional parameters with opaque tuple shapes make every call
     site hard to read.  A single DisplayScoreData dataclass would make
     call sites self-documenting and eliminate the ScoreHeader/FooterLine/
     FooterData/VecLabel aliases.
  5) [X] **OrderedDict → dict**
     Python 3.7+ guarantees dict insertion order.  OrderedDict in
     metric.py and vulnerability.py adds complexity with no benefit.
     Replacing it simplifies type annotations and removes an import.
  6) [X] **assert → raise ValueError**
     assert guards in Metric.__init__ and index.setter are silenced by
     python -O.  Replace with explicit raises for correct runtime
     behaviour in all execution modes.
  7) [X] **lambda/map → list comprehensions in vulnerability.py**
     Two map(lambda x: x.split(":"), ...) patterns remain in
     _parse_vulnerability_string; list comprehensions are more readable
     and avoid the type: ignore comment on the OrderedDict constructor.

* <2026-05-08 Fri> Findings from argparse / dataclass / DRY refactors [3/3]
  1) [X] **`Metric.__values` still uses `OrderedDict`** — metric.py was not
     touched in the DRY refactor, so `OrderedDict[str, MetricValue]` remains.
     Replacing with `dict[str, MetricValue]` completes TODO item 5 for the
     whole codebase and removes the `collections` import from metric.py.
  2) [X] **`cvs_factory` `type: ignore[call-arg]`** — The TypeVar `_T` cannot
     guarantee that `cls` has an `__init__(self, list[Metric]) -> None`
     signature.  Introduce a `CVSSConstructor` Protocol that declares exactly
     this, replace `type[_T]` with `type[CVSSConstructor]`, and return
     `CVSSConstructor`.  This removes the ignore comment and makes the
     factory's contract explicit.
  3) [X] **`_parse_vulnerability_string` `type: ignore[arg-type]`** — The
     `dict(pairs)` call requires `list[tuple[str, str]]` but `pairs` is
     `list[list[str]]` (split produces lists).  Using a dict comprehension
     `{p[0]: p[1] for p in pairs}` avoids the ignore and makes the intent
     clearer.  Can validate length-2 splits here too, eliminating the
     separate `ValueError` branch.


* <2014-06-30 Mån> [0/2]
  - [ ] Fixed documentation.
  - [ ] Use click instead of docopt
    - [ ] This will change the structure
    - [ ] Tests will need to be updated
    - [ ] New possibility to do tests in a different way?
  - [ ] How to push a tag?

* <2014-06-29 Sön> [5/5]
  - [X] Fixed som PEP issues.
  - [X] Convert to a package
  - [X] Refactor test scripts
  - [X] Update README.md
  - [X] Update DESCRIPTION.rst

* <2014-06-29 Sön>
Use git flow approach.  A simple reminder is

   http://jeffkreeftmeijer.com/2010/why-arent-you-using-git-flow/

* <2014-05-31 Lör>
Refactoring according to TODO.
* <2014-05-30 Fre>
** DONE Refactor code to also work with python2.7
** DONE Bump version to 1.20 and push these changes
* <2014-03-09 Sön>
** Plan and follow through some ToDo items
   Start using some simple org-mode TODO commands.
* <2014-03-08 Sat>
** DONE PEP8 checking
** TODO Different scores need refactoring [1/4]
   + [ ] Use DRY on vulnerability.py and cvss.py to factor out data
   related to scores. 
     - Currently quite a lot of repeated data.  For example see
       BASE_SCORE and base_metrics()
     - One problem is the mix of basic data and information used for
       the interactive user interface.
     - The mix of lists and tuples when initializing all Metrics
       works, but is not very clear and a bit diffcult to change
       because of duplication and repetition.
   + [X] First: move them all to the same file and make that work

   + [ ] Second: rearrange the data so that the info is more clearly
       structured.

   + [ ] DRY enough?

** TODO Refactor cvss.py [0/5]
   + [ ] Should contain only the logic for the command line syntax.
     The rest should be factored out into other files.
   + [ ] Change from docopt to click for more precision
   + [ ] Reading and processing input interactively
   + [ ] Display calculated scores
   + [ ] Setup and creation of a VulnerabilityVector
   + [ ] Setup and creation of the CommonVulnerabilityScore

* <2014-03-07 Fri>
** DONE Should transform this to a packgage with cvss.py [4/4]
   - [X] Why a package?  So I can distribute it via PyPi
   - [X] cvss.py the main program
   - [X] The examples should no longer need soft links to run
   - [X] Should be possible to package cvss.py as a stand alone program
** DONE test_vulnerability.py och vulnerability.md should move [3/3]
   - [X] Moved to directory examples
   - [X] How should the doctests import?
   - [X] How can these imports be made location independent?
** DONE README.md needs to be updated to reflect new structure
** DONE Scripts in tests need to be refactored [8/8]
In particular they should be made more location independent.

   - [X] test_uc01.sh
   - [X] test_uc02.sh
   - [X] test_uc03.sh
   - [X] test_uc04.sh
   - [X] test_uc05.sh
   - [X] test_uc06.sh
   - [X] run_all_tests.sh
   - [X] run_doctests.sh

** DONE General clean-up and restructuring
** DONE Commit the changes that were made in november.
   Completed, but maybe not in a perfect way.  Still need to
   understand how git flow och github interacts.
* 2013-11-14
** Extract VulnerabilityVector from cvss.py
Break the dependency on cvss_210 used for checking.

Tested. DONE.

* 2013-11-12
With the command line interface stablized, focus should now shift to
ensure that the code better reflects the problem domain.  A
vulnerability vector and the corresponding score algorithm are the
central items.  Interactive input of a vector and output in different
formats should use these abstractions.

So, the vulnerability is the container and the
CommonVulnerabilityScore is the algorithm that caculates the score
given a vulnerability vector.

DRY and SHY and Tell the other guy.

* 2013-11-12
** base_metrics() and ilk functions should be classes
Extract these to metric.py (pdding, prepare factory, select, read_and_set).

** display and output should move to separate class or file

** refactor main program
   More functions. Keep knowledge about docopt in main program.

** vulnerability_vector could also be a class
Done, but need to be moved inte separate file.

* 2013-11-09
** UseCases incoporated.
** Installation instructions updated.

* 2013-11-08
Next step?  Would like to have better interactive features.  Use cmd?

Less code in main program.  Should read more easily.
** parse a vector interactively and return a score
   when complete otherwise allow for interactive input
** Start using cmd
    Convert the main-loop to use cmd.Cmd when interactive.  Print and
    vector as data is completed.

    score
    base
    temporal
    environmental
      caclulate scores and print vectors

    base_parameters args*
    temporal_parameters args
    environmental_parameters
      check args use acceptable ones and loop over remaining
** rst output
   Generate verbose output in a RST format.
** plain text output
* 2013-11-07
** Refactor using cmd and docopt
*** Refactor cli (cli2)
    Simplify!  Tested. DONE.
*** Normalize output
   Print scores and vectors.  Tested. DONE.
*** Parse vectors and calculate scores
   Score.  Use default values when none available.

   Extract score_from function.  Tested,  DONE.
* 2013-11-06
** Refactor using cmd and docopt (cmd_and_docopt)
*** Extract functions from main program
    Tested. DONE.
*** Start using docopt for basic command line control
    Add -v --verbose to print tables.  Default is to print scores and vectors.
    Tested. DONE.

*** Add more command line options
    Tested. DONE.
* 2013-11-02
** Command line interafce (cli)
   Create a parser for the available metrics.  The output should be a
   sequence of selected values that are ordered in the same way as the
   available metrics.
*** Reorganize source files, the main program should reside in cvss.py
    Tested. Done.
*** A cli with 14 is a bit much.  Do the interactive version first.
    Interactive implemented.  Tested. Done.
*** Create a public project on github.
    Done.
* 2013-11-02
** cvss_refactor_3
*** Template patterna
   The CommonVulnerabilityScore should be the base and the cvssv210
   should be the concrete class.

   Done.  Changed names and added version property. Tested.

** cvss_refactor_2
*** Add function to make addressing with short names possible.
    Make it private.  Tested. Done.
*** Extract formulas into a separate class
    Tested. Done.
* 2013-11-01

** cvss_refactor_1
   DRY when it it comes to initial metric values and metric short
   names.  As the metrics are prepared the mapping could be
   constructed.

    Metric changed. CommonVulnerabilityScore changed.  No more DRY.
    Added short_name in input data with Metric.short_name solved this.
    Tested. Done.

** metric_refactor_2
*** cvs_factory and helper functions should be simpler
Magic number should be removed.  Helpers do too much.  Let them just
encapsulate the values. Pad selected and loop using zip an enumerate.
*** Keep initial data in helper functions.
Change factory to refelct this. Done. Tested.
*** Refactor cvs_factory
    No good calling pop on selected.  Extracted helper functions.
    Moved cvs_factory and helper functions to cvss.py
*** Refactor CommonVulnerabilityScore ctor
    Moved the mapping to a helper function.
* 2013-10-31
** CommonVulnerabilityScore
Should use the template pattern for the different scores and behave
like a dict that has a fixed set of keys with a Metric value.  How
should these keys be initialized and where?  DRY.
** metric_refactor_1
A Metric has a name, a string value and a float value and can can be
in one of several different states.  These states are set when the
Metric is constructed.  Once initilized, different states can be
selected but otherwise the Metric can not be changed.
*** Indexing use string key
Change so that indexing is done by a string key that is one of the
allowed values.  It is not robust to have the order of intialization
controlling how we set the value.  Use OrdereDict?

Tested. Done.

**
** metric_refactor_2
*** cvs_factory and helper functions should be simpler
Magic number should be removed.  Helpers do too much.  Let them just
encapsulate the values. Pad selected and loop using zip an enumerate.

* 2013-10-30
** Need to decide on clearer way to store Metrics and their values.
Done, for now.

** Recreate the tests from the examples in the specification.
Done.

** CommonVulnerabilityScore is constructed with a set of Metrics.
It can calculate the different scores and the respective vectors.

These have been implemented in a first version.  Looking at how
MetricVAlue and a Metric is constructed it is not good that the
formulas use index values that must be given on construction.  This
mapping should be moved to the class that uses the index values.  The
problem is that the metric short name is used inside Metric.  To make
this happen do the following:
*** Create a mapping in CommonVulnerabilityScore use it internally.
Created mapping from Metric to short name for matric.  Tested. Done.
*** Remove the need for short_name in Metric.
Removed short_name in Metric.  Tested. Done
*** Change interface of Metric.
Fixed. Tested. Done. 

** Parsing a command line or interactive input is a different matter.
Need a factory for CommonVulnerabilityScore to ease the pain of
building. Let it take a number of Metrics with their respective
MetricValues.  Fix the at a later stage.

*** cvs_factory
    First version in place.  Added a default test.  Tested.  Done.
*** cvs_factory refactored
    Group into three groups.  Loop. Tested. Done.
    Not too happy about this one.
*** cvs_factory refactored
    Does not look right.  Need a simpler way.  I want it to be easier
    to set the actual value of a metric.  Is there a better data
    structure for this.  Maybe dict or namedtuple?
*** cvs_factory refactored II
    Function moved over to metric.py.  Less coupling.
    Tested. Done.
*** cvs_factory refactored III
    Metric refactored.  Find a way to make selections in a cvs.
    Tested. All tests changed. Done.

*** What should the data look like to build the Metrics?
*** Use namedtuple for MetricValue and or Metric?
*** update tests

* 2013-10-29
** CVSS, how does it work?
Base metrics are assigned values, the base equation calculates a score
ranging from 0 to 10, and a vector is created.  The vector facilitates
the "open" nature of the framework.  It is a text string that contains
the values assigned to each metric, and it is used to communicate
exactly how the score for each vulnerability is derived.

The base score can be refined by assigning values to the temporal and
environmental metrics.  This is useful in order to provide additional
context for a vulnerability by more accurately reflecting the risk
posed by the vulnerability to a user's environment. 

For a temporal score, the temporal equation will combine the temporal
metrics with the base score to produce a temporal score ranging from 0
to 10. Similarly, if an environmental score is needed, the
environmental equation will combine the environmental metrics with the
temporal score to produce an environmental score ranging from 0 to 10.

** Base, Temporal, Environmental Vectors

Each metric in the vector consists of the abbreviated metric name,
followed by a ":" (colon), then the abbreviated metric value. The
vector lists these metrics in a predetermined order, using the "/"
(slash) character to separate the metrics. If a temporal or
environmental metric is not to be used, it is given a value of "ND"
(not defined). The base, temporal, and environmental vectors are shown
below in Table 13.

Metric Value	Description
Base	       AV:[L,A,N]/AC:[H,M,L]/Au:[M,S,N]/C:[N,P,C]/I:[N,P,C]/A:[N,P,C]
Temporal	   E:[U,POC,F,H,ND]/RL:[OF,TF,W,U,ND]/RC:[UC,UR,C,ND]
Environmental  CDP:[N,L,LM,MH,H,ND]/TD:[N,L,M,H,ND]/CR:[L,M,H,ND]/ IR:[L,M,H,ND]/AR:[L,M,H,ND]

Table 13: Base, Temporal and Environmental Vectors

For example, a vulnerability with base metric values of "Access
Vector: Low, Access Complexity: Medium, Authentication: None,
Confidentiality Impact: None, Integrity Impact: Partial, Availability
Impact: Complete" would have the following base vector:
"AV:L/AC:M/Au:N/C:N/I:P/A:C."

** Equations
Scoring equations and algorithms for the base, temporal and
environmental metric groups are described below. Further discussion of
the origin and testing of these equations is available at
http://www.first.org/cvss.  There are three set of Equations:

*** Base Equation
The base equation is the foundation of CVSS scoring. The base equation
(formula version 2.10) is:

**** BaseScore      = round_to_1_decimal(
                    (0.6*Impact + 0.4*Exploitability - 1.5)*
                    f(Impact)
                 )

**** Impact         = 10.41*(1-(1-ConfImpact)*(1-IntegImpact)*(1-AvailImpact))
**** Exploitability = 20 * AccessVector * AccessComplexity * Authentication
**** f(Impact)= 0 if Impact=0, 1.176 otherwise
**** AccessVector     = case AccessVector of
                        requires local access: 0.395
                        adjacent network accessible: 0.646
                        network accessible: 1.0

**** AccessComplexity = case AccessComplexity of
                        high: 0.35
                        medium: 0.61
                        low: 0.71

**** Authentication   = case Authentication of
                        requires multiple instances of authentication: 0.45
                        requires single instance of authentication: 0.56
                        requires no authentication: 0.704

**** ConfImpact       = case ConfidentialityImpact of
                        none:             0.0
                        partial:          0.275
                        complete:         0.660

**** IntegImpact      = case IntegrityImpact of
                        none:             0.0
                        partial:          0.275
                        complete:         0.660

**** AvailImpact      = case AvailabilityImpact of
                        none:             0.0
                        partial:          0.275
                        complete:         0.660
*** Temporal Equation
If employed, the temporal equation will combine the temporal metrics
with the base score to produce a temporal score ranging from 0
to 10. Further, the temporal score will produce a temporal score no
higher than the base score, and no greater than 33% lower than the
base score. The temporal equation is:

**** TemporalScore = round_to_1_decimal(BaseScore*Exploitability
                *RemediationLevel*ReportConfidence)

**** Exploitability   = case Exploitability of
                        unproven:             0.85
                        proof-of-concept:     0.9
                        functional:           0.95
                        high:                 1.00
			not defined:          1.00

**** RemediationLevel = case RemediationLevel of
                        official-fix:         0.87
                        temporary-fix:        0.90
                        workaround:           0.95
                        unavailable:          1.00
                        not defined:          1.00

**** ReportConfidence = case ReportConfidence of
                        unconfirmed:          0.90
                        uncorroborated:       0.95
                        confirmed:            1.00
                        not defined:          1.00

*** Environmental Equation
If employed, the environmental equation will combine the environmental
metrics with the temporal score to produce an environmental score
ranging from 0 to 10. Further, this equation will produce a score no
higher than the temporal score. The environmental equation is:

**** EnvironmentalScore = round_to_1_decimal(
       (AdjustedTemporal+ (10-AdjustedTemporal)*CollateralDamagePotential)*TargetDistribution
)

**** AdjustedTemporal = TemporalScore recomputed with the BaseScores Impact
     sub-equation replaced with the AdjustedImpact equation

**** AdjustedImpact = min(10,10.41*(1-(1-ConfImpact*ConfReq)*(1-IntegImpact*IntegReq)
                 *(1-AvailImpact*AvailReq)))

**** CollateralDamagePotential = case CollateralDamagePotential of
                                 none:            0
                                 low:             0.1
                                 low-medium:      0.3
                                 medium-high:     0.4
                                 high:            0.5
                                 not defined:     0

**** TargetDistribution        = case TargetDistribution of
                                 none:            0
                                 low:             0.25
                                 medium:          0.75
                                 high:            1.00
                                 not defined:     1.00

**** ConfReq 	         = case ConfReq of
                        low:              0.5
                        medium:           1.0
                        high:             1.51
                        not defined:      1.0

**** IntegReq         = case IntegReq of
                        low:              0.5
                        medium:           1.0
                        high:             1.51
                        not defined:      1.0

**** AvailReq         = case AvailReq of
                        low:              0.5
                        medium:           1.0
                        high:             1.51
                        not defined:      1.0


** First Action Plan
*** Program
   The program reads an number of key-value pairs and from these
   calculates a score based on the values read and their respective
   weight.  It also prints a vulnerability vector.
*** How to test

     In http://www.first.org/cvss/cvss-guide.html there are
     a number of examples.  That can be used to verify.

     At http://nvd.nist.gov/cvss.cfm?calculator&adv&version=2 we can
     calculate scores using a web interface and get a number of
     vectors.  

     At http://jvnrss.ise.chuo-u.ac.jp/jtg/cvss/en/CVSSv2.html there
     is another calculator.  Can be sued to compare.

*** Next step
Rewrite code to use python3.  Add tests and a command line interface
using standard python modules.  When this is done add an interactive
mode.

*** Integartion with emacs
Use pymacs or write your own elsip interactive function.

*** Alternatives

docopt, 7 Python libs.

** First reading
   No documentation and a number of global variables.  Appear to break
   the DRY-principle in several places.

   A set of global variables (acc_vec, ...) where the order seems to
   matter according to a comment.  Why?

   Three functions and a *very* long main function.  Hard to modify
   and extend.

   *is_valid_input* :  mixing all metric_value abbreviations in one big
   if.  Cute and brittle.  Adding a metric means that the function has
   to change.

   *cvss_score* : input parameter *ib* not used?  Formula unclear and
   obfuscated.

   *find_risk* : not scalable and not very pythonic.

   *main* : just too long.  Separate presenation, calculation and logic.

   *no tests* : nada, niente, rien...
   
